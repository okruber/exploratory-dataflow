{
    "_comment": "This file allows you to optionally add additional metadata for the Flex Template.",
    "name": "test",
    "description": "description from metadata file",
    "parameters": [
      {
        "name": "pipeline_config",
        "label": "Pipeline Configuration",
        "helpText": "The path to the JSON configuration file containing pipeline parameters. Can be a GCS path (gs://) or local file path. Example: gs://your-bucket/configs/pipeline_config.json",
        "regexes": [
          "^(gs:\\/\\/[^\\n\\r]+\\.json|[^\\n\\r]+\\.json)$"
        ]
      }
    ]
  }